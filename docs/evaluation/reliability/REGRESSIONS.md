# Regression fixes and deployment state

- Replaced prompt-only JSON formatting with Anthropic's schema-constrained outputs for mapping, critic, and synthesis (prompt/schema version 2.2.1). Pydantic still enforces numeric and semantic checks locally. This prevents misspelled/missing required output fields; it does not guarantee the observations are correct.
- The original unrelated-image failure remains in `unrelated.json`. A separate post-fix run in `regressions/unrelated-structured.json` completed in 37.67 seconds and returned needs-review/uncertain, explicitly explaining that the circuit illustration is not an agricultural photograph. Its suitability value was still `limited` rather than the preferred `unsuitable`; this semantic inconsistency remains an evaluation finding, despite the appropriate human-review action.
- A separate healthy-leaf browser check exposed unbalanced critic JSON before structured outputs were enabled. That UI error was visible and did not create a verdict. It is separate from the six-case paired comparison.
- The revised policy distinguishes uncertainty from a verified crop concern, requests a new photograph for unsuitable inputs, and converts an explicit absence of visible concern to NOMINAL without claiming whole-plant health.
- The workspace now shows real elapsed time and the current stage, and completed stages remain visibly checked. The printable report includes visible-concern and suitability assessments.
- Across the five completed initial pairs, average full latency was about 30.9 seconds versus a derived 24.3 seconds without the critic. Categories agreed in all five. The critic adds review notes and an enforcement point, but this evaluation does not demonstrate an accuracy advantage. Do not advertise superior accuracy from the critic. Consider making the review pass optional after a larger independently reviewed comparison.
- Local regression checks: 58 backend tests, report-export test and production frontend build pass. No public deployment was made during this evaluation.

The post-fix browser rerun of healthy case `absent-2` completed in 33.7 seconds with all four stages visibly checked and an explicit “NO VISIBLE CONCERN” finding. The model retained appropriate limitations about a single upper-leaf photograph. This verifies the repaired UI path but is not a new independent sample or a full repeat of the six-case comparison. The configured app now runs prompt/schema version 2.2.1.
